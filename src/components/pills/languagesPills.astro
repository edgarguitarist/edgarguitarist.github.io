---
interface Props {
    name: string;
}

const { name } = Astro.props;
---

<button title={name} class="text-xs rounded-full px-4 py-2  bg-slate-600 text-white hover:text-yellow-400 font-semibold hover:scale-110">{name}</button>